# SitePulse RAG

Realtime ingestion, chunking, embedding, and retrieval pipeline.

Planned components:
- Chunker (semantic + sliding window)
- Embeddings (nomic-embed-text via Ollama)
- Vector store (ChromaDB)
- Hybrid retrieval + metadata filters
