export default function InsightsPanel({ title, items }) {
  return (
    <div className="insights">
      <h4>{title}</h4>
      <ul>
        {items.map((item) => (
          <li key={item}>{item}</li>
        ))}
      </ul>
    </div>
  );
}
