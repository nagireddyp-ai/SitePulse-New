# Mock ServiceNow API

Local mock ITSM service for incidents and service requests.
Planned endpoints:
- GET /incidents
- POST /incidents/update
- GET /service-requests
- POST /service-requests/resolve
